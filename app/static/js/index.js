const CategoryList = function(items) {
    const self = this;

    self.categories = ko.observableArray(items);

    self.createCategory = function() {
    };

    self.deleteCategory = function() {
    }
};
